# Gradient Ascent setup prompt

Paste into a new Codex task in the Gradient Ascent checkout:

> Set up Gradient Ascent prompt-driven, asking one rider question at a time. Reuse `.venv/bin/gradient-ascent` and enabled `gradient-ascent@local`. If needed, require Python 3.11+ with venv support; run `python3 -m venv .venv`, `.venv/bin/python -m pip install --require-hashes -r requirements-build.lock`, `.venv/bin/python -m pip install --require-hashes -r requirements.lock`, `.venv/bin/python -m pip install --no-build-isolation --no-deps -e .`, and `scripts/install-local-plugin.sh`. If the plugin changes, verify it, stop, and ask me to paste this same prompt into a new Codex task. Use the setup skill. Keep athlete data in a separate private workspace at my chosen path or `~/code/gradient-ascent-workspace`. Resume `onboarding-status --json`; preserve answers. Cover profile, goals, events, plan/no-plan, history/no-history, and optional recovery. Offer official Ride with GPS `ride` CLI sync or local files: an official Strava account archive, FIT/TCX/GPX, an official Garmin Connect account export, Apple Health `export.xml`, plan CSV/XLSX, or a local sync manifest from an optional companion. Ask before `ride setup`; add `--install` only with permission. Let me click the sign-in URL; OAuth and tokens stay with the vendor. Use `import-sync-manifest` for companion files; authentication stays in that companion. `refresh` syncs explicitly enabled Ride with GPS; `refresh --local-only` stays offline. Never request credentials, implicitly enable live Garmin/Strava, expose raw data in model context, or invent metrics. Build the Training Center, verify its `/api/health` workspace ID, and report the URL and next missing input.
