# Gradient Ascent setup prompt

Paste this into a new Codex task opened from the Gradient Ascent checkout:

> Set up Gradient Ascent prompt-driven: handle shell/file work and ask one coherent rider question at a time. Reuse `.venv/bin/gradient-ascent` and enabled `gradient-ascent@local`. If needed, require Python 3.11+ with venv support and run `python3 -m venv .venv`, `.venv/bin/python -m pip install --require-hashes -r requirements-build.lock`, `.venv/bin/python -m pip install --require-hashes -r requirements.lock`, `.venv/bin/python -m pip install --no-build-isolation --no-deps -e .`, and `scripts/install-local-plugin.sh`. If the plugin changes, verify it, stop, and ask me to paste this same prompt into a new Codex task so skills load. Use the Gradient Ascent setup skill. Keep my private athlete workspace separate; use my requested path or `~/code/gradient-ascent-workspace`. Resume with `onboarding-status --json` and preserve answers. Cover profile, timezone, units, disciplines, experience, availability, constraints, goals, events, plan/no-plan, history/no-history, and optional recovery. Accept only local files: an official Strava account archive, FIT/TCX/GPX rides, an official Garmin Connect account export, Apple Health `export.xml`, plan CSV/XLSX, or a local sync manifest from an optional companion. Run `import-sync-manifest`, then `refresh`; authentication stays in the separate companion. Never request credentials, expose raw exports or manifests in model context, or invent metrics. Build the Training Center, verify its `/api/health` workspace ID, and report the URL and next missing input.
