# Gradient Ascent workspace root.
COACH_WORKSPACE_DIR=.

# Optional: plugin checkout path for scripts not exposed through the CLI.
# GRADIENT_ASCENT_PLUGIN_DIR=~/code/gradient-ascent

# Legacy alias still supported by the plugin.
# COACH_DATA_DIR=.

# Gradient Ascent imports local files and does not read provider credentials.
