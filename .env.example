COACH_WORKSPACE_DIR=~/code/gradient-ascent-workspace
# Optional: plugin checkout path for workspace refresh scripts.
# GRADIENT_ASCENT_PLUGIN_DIR=~/code/gradient-ascent
# Legacy alias still supported:
# COACH_DATA_DIR=~/code/gradient-ascent-workspace

# Gradient Ascent imports local files and does not read provider credentials.
